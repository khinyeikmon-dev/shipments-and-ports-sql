## 🔍 Business Questions Answered

- How many ports are located in Europe?
- What was the total cargo volume handled by North American ports in 2019?
- What was the average shipment weight from South American ports in 2017?
- Which port had the longest average docking time?
- How did shipments to Asian ports change between 2012 and 2020?

## 🗄️ Data Setup

The dataset is provided as CSV files and loaded into a SQL Server database.

Steps:
1. Create tables using `schema.sql`
2. Load data from CSV files
3. Run analytical queries from `queries.sql`

This mirrors a real-world analytics workflow where raw data is ingested, structured, and queried.

